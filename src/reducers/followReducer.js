import { types } from "../types/types";

const initialState = {
  follows: [],
};

export const followReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
